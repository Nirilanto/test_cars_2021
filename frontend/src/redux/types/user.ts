export interface IUser extends Document {
  email: string;
  nom: string;
  prenom: string;
  userId: string;
}

export interface Icars extends Document {
  email: string;
  nom: string;
  prenom: string;
  userId: string;
}
